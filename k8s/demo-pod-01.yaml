apiVersion: v1
kind: Pod
metadata:
  name: demo-pod
spec:
  containers:
    - image: hoangnguyen147/sample-node-demo
      name: sample-node-container
      ports:
        - containerPort: 3000
          protocol: TCP
